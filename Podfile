# Uncomment the next line to define a global platform for your project
platform :ios, '11.3'

target 'Find my pet' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Find my pet
pod 'Kingfisher', '~> 4.0'
pod 'Alamofire', '~> 4.7'
pod 'SwiftyJSON', '~> 4.0'
end
